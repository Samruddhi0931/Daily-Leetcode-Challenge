# 🟢 1. Two Sum

## 📌 Problem Statement

Given an array of integers `nums` and an integer `target`, return the **indices** of the two numbers such that they add up to `target`.

- You may assume that each input has **exactly one solution**.
- You may not use the same element twice.
- You can return the answer in any order.

---

## 🧾 Examples

### Example 1
```
Input: nums = [2,7,11,15], target = 9
Output: [0,1]
```
Explanation:  
`nums[0] + nums[1] == 9`

---

### Example 2
```
Input: nums = [3,2,4], target = 6
Output: [1,2]
```

---

### Example 3
```
Input: nums = [3,3], target = 6
Output: [0,1]
```

---

## 🔒 Constraints

- `2 <= nums.length <= 10^4`
- `-10^9 <= nums[i] <= 10^9`
- `-10^9 <= target <= 10^9`
- Only one valid answer exists.

---

# 🚀 Approach 1: Brute Force (Nested Loop)

## 💡 Idea

Check every pair of numbers to see if they sum to the target.

---

## 🧑‍💻 Python Code

```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        n = len(nums)
        
        for i in range(n):
            for j in range(i + 1, n):
                if nums[i] + nums[j] == target:
                    return [i, j]
```

---

## ⏱ Complexity

- Time: **O(n²)**
- Space: **O(1)**

⚠️ Not efficient for large inputs.

---

# 🚀 Approach 2: Optimal Solution (Hash Map) ✅

## 💡 Idea

Instead of checking every pair:

1. Traverse the array.
2. For each number, compute its complement:
   ```
   complement = target - num
   ```
3. If complement already exists in the hash map → solution found.
4. Otherwise, store current number with its index.

---

## 🧑‍💻 Python Code

```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        seen = {}
        
        for i, num in enumerate(nums):
            complement = target - num
            
            if complement in seen:
                return [seen[complement], i]
            
            seen[num] = i
```

---

## ⏱ Complexity (Optimal)

- Time: **O(n)**
- Space: **O(n)**

✅ Uses hashing for constant-time lookups.

---

# 📚 Key Takeaways

- Brute force works but is slow: **O(n²)**.
- Hash maps reduce time complexity to **O(n)**.
- Always think in terms of:
  > "Have I already seen the complement?"

---

# 📅 Daily LeetCode Log

- Day: 4
- Topic: Arrays + Hashing
- Difficulty: Easy
- Language: Python